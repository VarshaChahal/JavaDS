HashTable:
    key-value pairs
    keys are not ordered

Fast for the following operations:
    finding values
    adding new values 
    removing values

Good hash function:
    fast(constant time)
    returns the same result everytime (deterministic)
    distributes outputs uniformly, no hotspot in the hashtable

